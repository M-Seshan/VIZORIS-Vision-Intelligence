# VIZORIS Environment Configuration Template
PORT=5000

# MongoDB URI (Optional: If empty, uses persistent JSON storage in server/data/real_inspections.json)
MONGODB_URI=mongodb://localhost:27017/vizoris

# AI Vision Configuration
MIN_CONFIDENCE_THRESHOLD=80
AI_BLUR_THRESHOLD=60.0
NODE_ENV=development
